[phases.setup]
nixPkgs = ["chromium", "chromedriver", "nodejs_22", "python313"]

[phases.install]
cmds = [
  "pip install -r backend/requirements.txt -r backend/third_party/flights_monitor/requirements.txt",
  "npm install -g @fly-ai/flyai-cli@1.0.16"
]
